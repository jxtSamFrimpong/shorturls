const NavigationBar = ({})=> {
    return (
        <nav>
            <div class="logo">
                <div class="logo-circle"></div>
                SHORTEN.IT
            </div>
            <button class="login-btn">LOGIN</button>
        </nav>
    )
}

export default NavigationBar